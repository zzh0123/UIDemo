<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".recyclerview.RecyclerPlusReduceActivity">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintBottom_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginBottom="40dp"/>

    <ImageView
        android:id="@+id/iv_all_check"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@mipmap/unchecked"
        android:padding="3dp"
        app:layout_constraintTop_toBottomOf="@+id/recyclerView"
        app:layout_constraintLeft_toLeftOf="@+id/recyclerView"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:text="全选"
        android:textColor="@color/c_272848"
        android:textSize="16sp"
        android:layout_marginLeft="8dp"
        app:layout_constraintLeft_toRightOf="@+id/iv_all_check"
        app:layout_constraintTop_toTopOf="@+id/iv_all_check"
        app:layout_constraintBottom_toBottomOf="@+id/iv_all_check"/>

</androidx.constraintlayout.widget.ConstraintLayout>